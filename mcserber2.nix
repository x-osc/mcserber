# Auto-generated by compose2nix.

{ pkgs, lib, ... }:

{
  # Runtime
  virtualisation.docker = {
    enable = true;
    autoPrune.enable = true;
  };
  virtualisation.oci-containers.backend = "docker";

  # Containers
  virtualisation.oci-containers.containers."mcserber-mc" = {
    image = "itzg/minecraft-server:latest";
    environment = {
      "DIFFICULTY" = "2";
      "ENABLE_COMMAND_BLOCK" = "true";
      "ENABLE_WHITELIST" = "true";
      "EULA" = "TRUE";
      "HIDE_ONLINE_PLAYERS" = "true";
      "ICON" = "https://cdn.discordapp.com/icons/1447411301411197001/25b44cf084feee778e43d0fff987c27c.webp?size=512";
      "MEMORY" = "5120M";
      "MODRINTH_LOADER" = "fabric";
      "MODRINTH_MODPACK" = "./serberpack.mrpack";
      "MOTD" = "ebic minceraft serber";
      "ONLINE_MODE" = "false";
      "RESOURCE_PACK" = "https://download.mc-packs.net/pack/043db42f771efa012d69b83a4d776ae08eb1c83c.zip";
      "RESOURCE_PACK_PROMPT" = "pls download thx";
      "RESOURCE_PACK_REQUIRED" = "true";
      "RESOURCE_PACK_SHA1" = "043db42f771efa012d69b83a4d776ae08eb1c83c";
      "SEED" = "4958145370078478047";
      "SIMULATION_DISTANCE" = "6";
      "TYPE" = "MODRINTH";
      "USE_MEOWICE_FLAGS" = "true";
      "VERSION" = "1.21.10";
      "VIEW_DISTANCE" = "8";
    };
    volumes = [
      "/data/mc:/data:rw"
    ];
    ports = [
      "25565:25565/tcp"
    ];
    log-driver = "journald";
    extraOptions = [
      "--network-alias=mc"
      "--network=mcserber_default"
    ];
  };
  systemd.services."docker-mcserber-mc" = {
    serviceConfig = {
      Restart = lib.mkOverride 90 "no";
    };
    after = [
      "docker-network-mcserber_default.service"
    ];
    requires = [
      "docker-network-mcserber_default.service"
    ];
    partOf = [
      "docker-compose-mcserber-root.target"
    ];
    wantedBy = [
      "docker-compose-mcserber-root.target"
    ];
  };

  # Networks
  systemd.services."docker-network-mcserber_default" = {
    path = [ pkgs.docker ];
    serviceConfig = {
      Type = "oneshot";
      RemainAfterExit = true;
      ExecStop = "docker network rm -f mcserber_default";
    };
    script = ''
      docker network inspect mcserber_default || docker network create mcserber_default
    '';
    partOf = [ "docker-compose-mcserber-root.target" ];
    wantedBy = [ "docker-compose-mcserber-root.target" ];
  };

  # Root service
  # When started, this will automatically create all resources and start
  # the containers. When stopped, this will teardown all resources.
  systemd.targets."docker-compose-mcserber-root" = {
    unitConfig = {
      Description = "Root target generated by compose2nix.";
    };
    wantedBy = [ "multi-user.target" ];
  };
}
